(define (domain domain-name)

  (:requirements 
    :typing
  )

  (:types
    subtype1 subtype2 subtype3 - object
  )

  (:predicates
    (predicateName ?x - object ?y - object) 	
  ) 

  (:action action-name
    :parameters ()
    :precondition ()
    :effect ())
)